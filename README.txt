HOW TO USE
1. Open index.html in any browser
2. Edit text content directly in the HTML
3. Change colors by editing --primary-color in the :root section at the top of the CSS
4. Replace the hero image URL with your own image link